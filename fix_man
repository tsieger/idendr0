#!/bin/sh
# Fix man files generated by 'inlinedocs'. 
# The purpose is to:
# i) make long lines shorter
# ii) wrap interactive examples into '\donttest{}'

# cd man && find . -name "*.patch" | while read PTCH; do patch -f < ${PTCH}; done
cd man && patch -f < idendro.Rd.patch
